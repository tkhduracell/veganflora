{
  "devDependencies": {
    "firebase-tools": "^14.7.0",
    "turbo": "^2.5.3"
  },
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "format": "pnpm -r format --write",
    "lint": "pnpm -r lint --write",
    "check": "pnpm -r check",
    "build": "pnpm -r build"
  }
}
